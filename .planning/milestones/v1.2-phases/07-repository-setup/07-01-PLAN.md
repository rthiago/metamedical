---
phase: 07-repository-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.gitignore]
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02]

must_haves:
  truths:
    - "A private GitHub repository named 'metamedical' exists under the user's account"
    - "The repository contains the full codebase (all HTML, CSS, JS, and assets)"
    - "Complete git commit history from v1.0 through v1.1 is preserved in the remote"
    - "The master branch is the default branch on GitHub"
  artifacts:
    - path: ".gitignore"
      provides: "Standard gitignore for the project"
    - path: "remote: origin"
      provides: "GitHub remote pointing to rthiago/metamedical"
  key_links:
    - from: "local master branch"
      to: "origin/master on GitHub"
      via: "git push -u origin master"
      pattern: "git remote -v shows github.com"
---

<objective>
Create a private GitHub repository and push the full codebase with complete git history.

Purpose: Establish the MetaMedical website source code on GitHub as the single source of truth, enabling GitHub Pages deployment in Phase 8.
Output: Private GitHub repository at github.com/rthiago/metamedical with all code and full commit history.
</objective>

<execution_context>
@/home/thiago/.claude/get-shit-done/workflows/execute-plan.md
@/home/thiago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review and create .gitignore</name>
  <files>.gitignore</files>
  <action>
    Review the working tree for any files that should be excluded from version control. The user decided to include `.planning/` directory (push everything including planning docs) and confirmed no sensitive files exist.

    Create a minimal `.gitignore` appropriate for a static HTML/CSS/JS site:
    - OS files: `.DS_Store`, `Thumbs.db`
    - Editor files: `.vscode/`, `.idea/`, `*.swp`, `*.swo`
    - No node_modules (not a Node project)
    - Do NOT ignore `.planning/` — user decision to include it

    Stage and commit the .gitignore file.
  </action>
  <verify>
    `cat .gitignore` shows the expected patterns.
    `git status` shows clean working tree after commit.
  </verify>
  <done>.gitignore exists with appropriate patterns, committed to master.</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub repository and push codebase</name>
  <files></files>
  <action>
    Use the GitHub CLI (`gh`) to create the repository and push:

    1. Create private repository:
       `gh repo create metamedical --private --description "MetaMedical - Medical equipment and supplies website" --source . --remote origin`
       This creates the repo AND adds the origin remote in one command.

    2. Set master as default branch on GitHub:
       `gh api -X PATCH repos/rthiago/metamedical -f default_branch=master`

    3. Push master with full history:
       `git push -u origin master`

    4. Verify the push:
       - `git remote -v` shows origin pointing to rthiago/metamedical
       - `gh repo view rthiago/metamedical --json name,visibility,defaultBranch` confirms private, master default
       - `git log --oneline origin/master | wc -l` matches local commit count

    The repository description uses "MetaMedical - Medical equipment and supplies website" per Claude's Discretion on description text.
    No README is created — per user decision to skip it.
    No topics/tags — per user decision (private repo doesn't need discoverability).
  </action>
  <verify>
    `gh repo view rthiago/metamedical --json name,visibility,defaultBranch` returns name=metamedical, visibility=PRIVATE, defaultBranch=master.
    `git log --oneline origin/master | wc -l` matches `git log --oneline | wc -l` (full history pushed).
    `git remote -v` shows origin pointing to github.com rthiago/metamedical.
  </verify>
  <done>Private GitHub repository "metamedical" exists with full codebase and complete git history. Master is the default branch. No extra branches pushed.</done>
</task>

</tasks>

<verification>
1. `gh repo view rthiago/metamedical --json name,visibility,defaultBranch` — confirms repo exists, is private, default branch is master
2. `git log --oneline origin/master | wc -l` equals `git log --oneline | wc -l` — full history preserved
3. `git diff origin/master..master` — empty (no divergence)
4. `gh api repos/rthiago/metamedical/contents/ --jq '.[].name'` — lists top-level files/dirs confirming codebase is present
</verification>

<success_criteria>
- Private repository "metamedical" exists at github.com/rthiago/metamedical
- Repository contains the full codebase (HTML, CSS, JS, assets, .planning/)
- All 68+ commits from v1.0 through v1.1 are present in the remote
- Master is the default branch
- No other branches pushed
- Origin remote configured locally
</success_criteria>

<output>
After completion, create `.planning/phases/07-repository-setup/07-01-SUMMARY.md`
</output>

---
phase: 03-polish-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - empresa.html
  - produtos.html
  - localizacao.html
  - contato.html
  - css/style.css
autonomous: true
requirements:
  - DSGN-04
  - SEO-01
  - SEO-02
  - SEO-03

must_haves:
  truths:
    - "Every page has a unique <title> and meta description visible in page source"
    - "Every page has og:title, og:description, og:type, and og:url meta tags"
    - "HTML structure uses semantic elements (header, nav, main, section, footer) on all pages"
    - "Product cards, contact cards, nav links, and buttons show visible hover/transition effects"
    - "All files are static HTML/CSS with no server config needed"
  artifacts:
    - path: "index.html"
      provides: "Home page with SEO meta + OG tags"
      contains: "og:title"
    - path: "empresa.html"
      provides: "Empresa page with SEO meta + OG tags"
      contains: "og:title"
    - path: "produtos.html"
      provides: "Produtos page with SEO meta + OG tags"
      contains: "og:title"
    - path: "localizacao.html"
      provides: "Localizacao page with SEO meta + OG tags"
      contains: "og:title"
    - path: "contato.html"
      provides: "Contato page with SEO meta + OG tags"
      contains: "og:title"
    - path: "css/style.css"
      provides: "Enhanced hover/transition effects"
      contains: "transition"
  key_links:
    - from: "*.html"
      to: "css/style.css"
      via: "link rel=stylesheet"
      pattern: "link.*style\\.css"
---

<objective>
Add SEO meta tags and Open Graph tags to all 5 pages, enhance CSS hover/transition effects on interactive elements, audit semantic HTML, and confirm the site is deploy-ready as static files.

Purpose: Make the site discoverable by search engines, shareable on social media, and polished with smooth interactions.
Output: All 5 HTML pages with complete SEO metadata and a CSS file with enhanced hover/transition animations.
</objective>

<execution_context>
@/home/thiago/.claude/get-shit-done/workflows/execute-plan.md
@/home/thiago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-pages/02-01-SUMMARY.md
@.planning/phases/02-content-pages/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SEO meta tags and Open Graph tags to all pages</name>
  <files>index.html, empresa.html, produtos.html, localizacao.html, contato.html</files>
  <action>
For each of the 5 HTML pages, add inside the `<head>` section (after the existing `<title>` tag):

1. **Meta description** — unique per page describing the page content in Portuguese, 150-160 characters.
2. **Open Graph tags** — og:title, og:description, og:type (website), og:url, og:locale (pt_BR).

Page-specific meta content:
- **index.html**: Title already "Home | MetaMedical". Description about MetaMedical being a medical supplies company. OG title: "MetaMedical - Equipamentos e Suprimentos M&eacute;dicos"
- **empresa.html**: Description about company history and values. OG title: "Empresa | MetaMedical"
- **produtos.html**: Description about Bleed STP, Pin&ccedil;a Bipolar, Bleed STP+ product line. OG title: "Produtos | MetaMedical"
- **localizacao.html**: Description about location in Osasco/SP. OG title: "Localiza&ccedil;&atilde;o | MetaMedical"
- **contato.html**: Description about contact information. OG title: "Contato | MetaMedical"

Also audit semantic HTML (SEO-02): Verify each page uses `<header>`, `<nav>`, `<main>`, `<section>`, `<footer>`. If any page is missing `<main>` wrapping its content sections, add it. The existing pages from phases 01-02 should already have these, but verify and fix any gaps.

Use HTML entities for Portuguese accented characters consistent with existing codebase pattern.
  </action>
  <verify>
For each HTML file, run `grep -c 'meta name="description"'` (expect 1 per file) and `grep -c 'og:title'` (expect 1 per file). Also run `grep -c '<main'` on each file (expect 1 per file).
  </verify>
  <done>All 5 pages have unique meta description and full OG tags (og:title, og:description, og:type, og:url, og:locale). All pages use semantic HTML structure with header, nav, main, section, footer.</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS hover/transition effects on interactive elements</name>
  <files>css/style.css</files>
  <action>
Enhance CSS with subtle hover/transition effects for interactive elements. The CSS already has some transitions (nav links, buttons, logo). Add or enhance the following:

1. **Product cards** (.product-detail or similar): Add `transition: transform 0.3s ease, box-shadow 0.3s ease;` and on hover: slight translateY(-4px) lift + enhanced box-shadow.

2. **Contact cards** (.contact-card): Same lift effect on hover — translateY(-2px) with subtle shadow increase.

3. **Value cards/items**: If .values-grid children exist, add subtle hover highlight (background-color transition or border-color change).

4. **Footer links**: Add hover color transition if not present.

5. **Section CTA buttons**: Verify .btn already has hover effect (it does), optionally add a slight scale(1.02) on hover for emphasis.

6. **Product preview cards** on home page: Add hover lift effect matching product cards.

Keep all transitions using the existing `--transition` CSS custom property (0.3s ease) for consistency. Do NOT add JavaScript — CSS only per project constraint.
  </action>
  <verify>
Open each page in a browser and hover over: nav links, product cards, contact cards, buttons, footer links. Each should show a visible transition effect (color change, lift, shadow, or scale). Verify no JavaScript was added: `grep -r 'script' *.html` should return 0 matches (excluding any existing non-JS script tags).
  </verify>
  <done>All interactive elements (product cards, contact cards, value items, buttons, nav links, footer links) have visible CSS hover/transition effects. No JavaScript added.</done>
</task>

</tasks>

<verification>
1. View page source of each HTML file and confirm unique title, meta description, and OG tags
2. Validate semantic HTML: each page has header > nav, main > section(s), footer
3. Hover over every clickable/interactive element across all 5 pages to confirm transition effects
4. Upload the project folder (html + css/) to a test server or open locally — all pages load correctly with no missing assets or broken links
5. No JavaScript files or inline scripts exist in the project
</verification>

<success_criteria>
- 5 unique meta descriptions across 5 pages
- 5 sets of OG tags (og:title, og:description, og:type, og:url, og:locale) across 5 pages
- Semantic HTML (header, nav, main, section, footer) verified on all pages
- Hover effects visible on product cards, contact cards, buttons, nav links
- Site works as static files with zero configuration
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-deploy/03-01-SUMMARY.md`
</output>

---
phase: 04-typography-spacing-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [css/style.css]
autonomous: true
requirements: [TYPO-01, TYPO-02]

must_haves:
  truths:
    - "Headings scale smoothly from mobile to desktop without any abrupt size jumps"
    - "Sections have visibly more breathing room than v1.0 — content never feels cramped"
    - "No heading font-size uses a fixed rem/px value — all use clamp()"
    - "Section padding is at least 6rem on desktop viewports"
  artifacts:
    - path: "css/style.css"
      provides: "Fluid type scale custom properties and generous spacing system"
      contains: "clamp("
  key_links:
    - from: "css/style.css :root"
      to: "h1, h2, h3, .hero h1, .tagline, body"
      via: "CSS custom property references"
      pattern: "var\\(--text-"
---

<objective>
Implement fluid typography with CSS clamp() and upgrade section spacing to premium levels.

Purpose: Replace all fixed heading/body font sizes with smooth viewport-responsive clamp() values, and increase section padding from 4rem to 6rem+ for generous breathing room. This is the typographic foundation that makes every page feel premium.

Output: Updated css/style.css with fluid type scale custom properties and generous spacing custom properties applied across all elements.
</objective>

<execution_context>
@/home/thiago/.claude/get-shit-done/workflows/execute-plan.md
@/home/thiago/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-typography-spacing-foundation/04-RESEARCH.md
@css/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fluid type scale and spacing custom properties</name>
  <files>css/style.css</files>
  <action>
In the `:root` block of css/style.css, replace the existing Typography and Spacing scale sections with fluid values:

**Typography — replace existing font vars, ADD these new custom properties:**
```css
/* Fluid type scale */
--text-base: clamp(1rem, 0.95rem + 0.25vw, 1.125rem);
--text-lg: clamp(1.125rem, 1rem + 0.5vw, 1.375rem);
--text-xl: clamp(1.25rem, 1.1rem + 0.75vw, 1.75rem);
--text-2xl: clamp(1.5rem, 1.2rem + 1.5vw, 2.25rem);
--text-3xl: clamp(1.875rem, 1.5rem + 1.875vw, 2.75rem);
--text-hero: clamp(2.25rem, 1.75rem + 2.5vw, 3.5rem);
```

**Spacing — keep existing --space-xs through --space-2xl, ADD:**
```css
--space-3xl: 6rem;
--space-4xl: 8rem;
```

Keep `--font-body` and `--font-heading` unchanged. Keep all color variables unchanged.
  </action>
  <verify>Grep css/style.css for "clamp(" — should find 6 occurrences. Grep for "--space-3xl" and "--space-4xl" — 1 occurrence each in :root.</verify>
  <done>All 6 fluid type custom properties and 2 new spacing properties exist in :root block.</done>
</task>

<task type="auto">
  <name>Task 2: Apply fluid sizes to all typography rules and upgrade section spacing</name>
  <files>css/style.css</files>
  <action>
**Apply type scale to elements — replace ALL fixed font-size declarations:**

1. `body` — change `font-size: 16px` to `font-size: var(--text-base)`
2. `h1` — change `font-size: 2.5rem` to `font-size: var(--text-3xl)`
3. `h2` — change `font-size: 2rem` to `font-size: var(--text-2xl)`
4. `h3` — change `font-size: 1.5rem` to `font-size: var(--text-xl)`
5. `p` — change `font-size: 1rem` to `font-size: var(--text-base)`
6. `.hero h1` — change `font-size: 3rem` to `font-size: var(--text-hero)`
7. `.hero .tagline` — change `font-size: 1.25rem` to `font-size: var(--text-lg)`
8. `.footer-section h3` — change `font-size: 1.1rem` to `font-size: var(--text-base)` (footer headings stay modest)
9. `.footer-section p`, `.footer-section a`, `.footer-bottom p` — keep small sizes (0.9rem/0.85rem) as-is since these are intentionally reduced

**Upgrade section spacing:**

10. `.section` — change `padding: var(--space-2xl) 0` to `padding: clamp(3rem, 2.5rem + 2.5vw, 6rem) 0` (fluid: 3rem mobile → 6rem desktop)
11. `.hero-content` — change `padding: var(--space-2xl) var(--space-sm)` to `padding: var(--space-3xl) var(--space-sm)` (hero gets premium spacing)

**REMOVE fixed font-size overrides in media queries:**

12. In `@media (max-width: 767px)` — REMOVE `h1 { font-size: 2rem; }` and `h2 { font-size: 1.5rem; }` rules (clamp handles this now)
13. In `@media (max-width: 767px)` — REMOVE `.hero h1 { font-size: 2rem; }` rule (clamp handles this)

Keep all other media query rules (grid columns, nav layout, etc.) intact.
  </action>
  <verify>
Grep css/style.css for fixed heading sizes — should find NO occurrences of "font-size: 2.5rem", "font-size: 2rem" (in heading rules), "font-size: 1.5rem" (in heading rules), or "font-size: 3rem". The only remaining fixed font-sizes should be small utility sizes (0.9rem, 0.85rem, 0.95rem in nav/footer).
  </verify>
  <done>All heading and body font-sizes use var(--text-*) fluid properties. Section padding is at least 6rem on desktop. Mobile media query heading overrides are removed. Typography scales smoothly from 320px to 1200px+ viewports.</done>
</task>

</tasks>

<verification>
1. Open index.html in browser at 1200px width — headings should be larger than v1.0 (h1 at ~44px, hero at ~56px)
2. Resize browser from 1200px to 375px — headings should shrink smoothly with NO jumps
3. Section padding should be visibly more generous than before (~96px on desktop vs previous 64px)
4. Footer text sizes should remain compact and unchanged
5. No layout breaks at any viewport width between 320px and 1920px
</verification>

<success_criteria>
- All 6 fluid type custom properties defined in :root
- All heading elements reference var(--text-*) properties
- Section padding uses clamp() for fluid spacing (3rem mobile → 6rem desktop)
- No fixed font-size values remain on headings or body
- Mobile media query heading overrides removed (clamp handles responsiveness)
- No visual regressions on any page
</success_criteria>

<output>
After completion, create `.planning/phases/04-typography-spacing-foundation/04-01-SUMMARY.md`
</output>
